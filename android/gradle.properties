org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# The app has no AndroidX dependency at all (plain Activity and WebView), so
# there is nothing to migrate or jetify.
android.useAndroidX=false
android.nonTransitiveRClass=true
